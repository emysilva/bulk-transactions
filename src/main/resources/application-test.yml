spring:
  datasource:
    url: jdbc:h2:mem:bulk_transactions_db;DB_CLOSE_DELAY=-1;DB_CLOSE_ON_EXIT=FALSE
    driver-class-name: org.h2.Driver
    username: sa
    password:
  jpa:
    hibernate:
      ddl-auto: create-drop
    properties:
      hibernate:
        dialect: org.hibernate.dialect.H2Dialect
    show-sql: true

  h2:
    console:
      enabled: true
      path: /h2-console

jwt:
  secret: h8jdMysWxPFLStqi/OzPDON0kJob/dXYaIPJg60X8Bg=
  expiration: 300000 # 5 minutes for testing

# Disable security telemetry/log spam during tests
logging:
  level:
    org.springframework.security: WARN
    org.hibernate.SQL: DEBUG
